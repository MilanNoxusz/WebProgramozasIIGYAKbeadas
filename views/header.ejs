<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <title>Éttermem</title>
    <link rel="stylesheet" href="/css/style.css"> 
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css">
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">Feane Étterem</a>
  
  <div class="collapse navbar-collapse">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item"><a class="nav-link" href="/">Főoldal</a></li>
      
      <% if (isAuthenticated) { %>
          <li class="nav-item"><a class="nav-link" href="/messages">Üzenetek</a></li>
      <% } %>

      <% if (isAuthenticated && currentUser.isAdmin == 1) { %>
          <li class="nav-item"><a class="nav-link text-danger" href="/admin">ADMIN</a></li>
      <% } %>
    </ul>

    <ul class="navbar-nav ml-auto">
      <% if (!isAuthenticated) { %>
          <li class="nav-item"><a class="nav-link" href="/login">Belépés</a></li>
          <li class="nav-item"><a class="nav-link" href="/register">Regisztráció</a></li>
      <% } else { %>
          <li class="nav-item"><span class="nav-link">Szia, <%= currentUser.username %>!</span></li>
          <li class="nav-item"><a class="nav-link" href="/logout">Kilépés</a></li>
      <% } %>
    </ul>
  </div>
</nav>
<div class="container mt-4"></div>