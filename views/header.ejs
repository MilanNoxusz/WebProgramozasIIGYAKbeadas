<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <title>Dante Pizzériája</title>
    <link rel="stylesheet" href="<%= appPath %>/css/style.css"> 
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css">
    <link href="<%= appPath %>/css/font-awesome.min.css" rel="stylesheet" />
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="<%= appPath %>/">Dante Pizzéria</a>
  
  <div class="collapse navbar-collapse">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item"><a class="nav-link" href="<%= appPath %>/">Főoldal</a></li>
      
      <li class="nav-item"><a class="nav-link" href="<%= appPath %>/offers">Ajánlataink</a></li>

      <% if (isAuthenticated) { %>
          <li class="nav-item"><a class="nav-link" href="<%= appPath %>/messages">Üzenetek</a></li>
      <% } %>
      <% if (isAuthenticated && currentUser.isAdmin == 1) { %>
          <li class="nav-item"><a class="nav-link text-danger" href="<%= appPath %>/admin">ADMIN</a></li>
      <% } %>
    </ul>

    <ul class="navbar-nav ml-auto">
      <% if (!isAuthenticated) { %>
          <li class="nav-item"><a class="nav-link" href="<%= appPath %>/login">Belépés</a></li>
          <li class="nav-item"><a class="nav-link" href="<%= appPath %>/register">Regisztráció</a></li>
      <% } else { %>
          <li class="nav-item"><span class="nav-link">Szia, <%= currentUser.username %>!</span></li>
          <li class="nav-item"><a class="nav-link" href="<%= appPath %>/logout">Kilépés</a></li>
      <% } %>
    </ul>
  </div>
</nav>